#ifndef UNIT_TEST_H
#define UNIT_TEST_H

/*
 * This function does not actually perform unit testing.
 * It just writes output of requested function to stdout
 * for examination by an external testing program - written
 * in Python in our case.
 */
int unit_testing(int argc, char **argv);

#endif
